{
  "scripts": {
    "check": "ncu -u --dep prod, dev",
    "upgrade": "ncu -u --dep prod,dev && npm update && npm install",
    "font-styles": "gulp fonts:process && gulp css:purge-libs && gulp css:process",
    "development": "gulp serve:dev",
    "production": "gulp serve:prod --production"
  }
}